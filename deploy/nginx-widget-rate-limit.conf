# Load in the nginx http context (for example from /etc/nginx/conf.d/).
limit_req_zone $binary_remote_addr zone=bert_widget_quote:10m rate=30r/m;
limit_conn_zone $binary_remote_addr zone=bert_widget_quote_conn:10m;
